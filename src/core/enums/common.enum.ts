export enum EAppEnvironment {
  Dev = 'develop',
  Prod = 'production',
}

export enum Sort {
  Asc = 'ASC',
  Desc = 'DESC',
}

export enum DateUnit {
  Day = 'day',
  Minute = 'minute',
  Second = 'second',
  Month = 'month',
}
